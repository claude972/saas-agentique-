[project]
name = "btp-reports"
version = "1.0.0"
description = "Services métier comptes-rendus — BTP Agent Platform"
requires-python = ">=3.11"
dependencies = ["btp-database"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["btp"]
