[project]
name = "btp-ao"
version = "1.0.0"
description = "Services métier appels d'offres — BTP Agent Platform"
requires-python = ">=3.11"
dependencies = ["btp-database", "btp-agents"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["btp"]
