[project]
name = "btp-llm-router"
version = "1.0.0"
description = "Routage multi-LLM (Claude / GPT / Gemini / Mistral) — BTP Agent Platform"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["btp"]
