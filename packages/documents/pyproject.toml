[project]
name = "btp-documents"
version = "1.0.0"
description = "Gestion documentaire (OCR, classement, stockage) — BTP Agent Platform"
requires-python = ">=3.11"
dependencies = ["btp-database"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["btp"]
